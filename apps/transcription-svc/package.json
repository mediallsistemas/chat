{
  "name": "transcription-svc",
  "version": "1.0.0",
  "private": true,
  "description": "Mediall transcription service — processes meeting recordings via Anthropic Claude. Communicates with the monolith over Redis Streams. See docs/17_PLANO_MODULAR_MONOLITH.md Fase 2.",
  "scripts": {
    "build": "tsc",
    "start": "node dist/main.js",
    "start:dev": "ts-node-dev --respawn --transpile-only src/main.ts",
    "lint": "tsc --noEmit",
    "test": "echo 'no tests yet'"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.1",
    "@mediall/contracts": "*",
    "@mediall/events": "*",
    "axios": "^1.7.0",
    "ioredis": "^5.0.0",
    "pino": "^9.5.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.4.0"
  }
}
