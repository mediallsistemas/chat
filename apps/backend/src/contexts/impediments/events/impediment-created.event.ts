// Canonical definition lives in shared/events so other contexts can consume it
// without a cross-context import. Re-exported here for backwards compatibility.
export { ImpedimentCreatedEvent } from '../../../shared/events'
